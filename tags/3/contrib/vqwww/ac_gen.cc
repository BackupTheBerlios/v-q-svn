#include <exception>
#include <iostream>
#include <fstream>
#include <string>
#include <cerrno>

#include "conf_home.h"

using namespace std;

void usage(const char *me) {
	cerr<<"usage: "<<me<<" type name"<<endl;
}

int main(int ac, char **av) {
	try {
			if(ac!=3) {
					usage(*av);
					return 1;
			}
		
			string ac_conf("auto/");
			ac_conf+=av[2];
			ac_conf+=".cc";
			
			ofstream out(ac_conf.c_str(), ios::trunc);
			out<<"// file autogenerated, don't edit!"<<endl
				<<"#include \"../cconf.h\""<<endl
				<<av[1]<<" ac_"<<av[2]<<"(\""<<conf_home
				<<"/etc/vqwww/"<<av[2]<<"\");"<<endl;

			out.close();
			if(! out) {
				cerr<<"can't write: "<<ac_conf<<": "<<strerror(errno)<<endl;
				return 1;
			}
	} catch( const exception & e ) {
			cerr<<"exception: "<<e.what()<<endl;
			return 1;
	}
	return 0;
}
